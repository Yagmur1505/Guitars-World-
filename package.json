{
  "name": "guitar-world-project",
  "version": "1.0.0",
  "description": "Guitar world company page",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm-run-all --parallel server sass",
    "server": "live-server --port=8080 --no-browser",
    "sass": "sass sass/main.scss style.css -w"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.79.5"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
